import React from 'react';

const ForestGump = () => (
  <div style={{ backgroundColor: '#1B1B1B', height: '658px', overflowY: 'auto' }} className="w-full p-8 rounded-lg">
    <h2 className="text-2xl font-semibold mb-4">The Script</h2>
    <p className="mb-4 text-lg font-light text-gray-400">EXT. BENCH - DAY</p>
    <p className="mb-4 font-bold">FOREST GUMP:</p>
    <p className="mb-4">I don't know if mama was right of it's Lieutenant Dan I don't know if we each have a destiny or if we're all just floating around, accidentally, like on a breeze.</p>
  </div>
);

export default ForestGump;
